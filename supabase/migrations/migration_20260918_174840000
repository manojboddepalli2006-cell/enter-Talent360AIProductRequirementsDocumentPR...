-- Video interview mode for practice sessions (same-business compatible change)
alter table public.talent_practice_sessions
  add column if not exists mode text not null default 'text';

update public.talent_practice_sessions set mode = 'text' where mode not in ('text', 'video');